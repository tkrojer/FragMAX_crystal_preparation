class check(object):
    def __init__(self, settingsObject, logger):
        self.settingsObject = settingsObject
        self.logger = logger

    # available tips

    # target plate

    # compound plate
    # warn if not emough compounds to soak all crystal drops in plate